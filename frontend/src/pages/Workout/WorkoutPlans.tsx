import React from "react";

type Props = {
	showLog: boolean;
	myWorkouts: boolean;
};

function WorkoutPlans({ showLog, myWorkouts }: Props): JSX.Element {
	return <div>WorkoutPlans</div>;
}

export default WorkoutPlans;
